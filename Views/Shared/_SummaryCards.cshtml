@* Renders a row of summary stat cards.
   Expects ViewData["Cards"] = List<(string Title, string Value, string Icon)> *@
@{
    var cards = ViewData["Cards"] as List<(string Title, string Value, string Icon)>
                ?? new List<(string, string, string)>();
}

<div class="row g-4 mb-4">
    @foreach (var card in cards)
    {
        <div class="col-12 col-sm-6 col-xl-3">
            <div class="card card-clean h-100">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p class="text-muted mb-1 small">@card.Title</p>
                        <h2 class="mb-0 fw-bold">@card.Value</h2>
                    </div>
                    <div class="d-flex align-items-center justify-content-center rounded-3"
                         style="width:56px;height:56px;background:#EFF6FF;">
                        <i class="bi @card.Icon" style="font-size:1.5rem;color:#1B4FD8;"></i>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
